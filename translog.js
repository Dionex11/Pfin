import React, { useState } from "react";
import { View,Text,Image,TextInput,ScrollView,StyleSheet, Button, Touchable, TouchableOpacity } from "react-native";
import styles from './styles';


const Translog=(len)=>{
    return(
        <View>
            <View style={styles.transbox}>
                <Text></Text>
            </View>
        </View>
    )
}
export default Translog;